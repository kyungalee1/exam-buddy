{
  "name": "exam-buddy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/copy-icons.mjs",
    "dev": "npm run build && npx --yes serve .",
    "setup": "node scripts/setup-supabase.mjs"
  },
  "devDependencies": {
    "pg": "^8.16.0"
  }
}
